import Vue from "vue";

// had to modify this from the original produced by the "vue add vuetify" command because the original did not work
// import Vuetify from "vuetify/lib/framework";
import Vuetify from "vuetify";

Vue.use(Vuetify);

export default new Vuetify({});
